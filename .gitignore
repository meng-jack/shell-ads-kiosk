build/bin
node_modules
frontend/dist
kiosk.json

# Launcher
launcher/launcher.exe
# launcher/static/assets/ is populated by CI (dash/dist → launcher/static)
# and must NOT be committed — it is embedded at build time.
launcher/static/assets

# Dashboard build output
dash/dist
dash/node_modules
